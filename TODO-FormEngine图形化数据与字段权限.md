# TODO：FormEngine 图形化数据范围与详情字段权限

> 状态：待设计、待排期  
> 适用范围：Microi 吾码 FormEngine、模块引擎、角色权限、PC、UniApp、SDK、接口引擎  
> 核心目标：让管理员通过图形界面配置数据范围和字段可见性，由后端统一编译、缓存并强制执行，不能只依赖前端隐藏。

## 一、当前已经确认的兼容语义

1. 客户端传入真实 `_SysMenuId` 查询列表时，严格应用该菜单的 `SqlWhere`、`SqlJoin` / `JoinTables`。
2. 客户端未传 `_SysMenuId` 查询列表时，从当前用户的版本化角色菜单缓存中找出所有直接绑定目标表且拥有查询权限的菜单：
   - 任一候选菜单没有数据范围时，允许读取整表。
   - 多个候选菜单的可访问范围按逻辑 OR 做安全合并。
   - 不同 Join 上下文不能被错误拼接；无法安全合并时必须失败关闭或进入专门的多策略查询执行器，不能因漏传 `_SysMenuId` 自动获得整表。
3. 查询单条详情时，只校验当前用户是否拥有直接绑定目标 DIY 表的菜单（或精确表级 `Read` 权限），暂不应用菜单列表的 `SqlWhere` / `SqlJoin`。
4. 新增、编辑、删除只校验同表菜单对应的 `Add`、`Edit`、`Del` 权限，不应用模块列表查询范围。需要“只能修改本人数据”等业务约束时，由后端表单 V8 或专用接口引擎负责。
5. 后端接口引擎、后端表单 V8、任务和平台内部代码属于服务端可信执行上下文，不要求客户端 `_SysMenuId`；其管理入口仍必须严格受控。
6. 平台保护表、租户边界、TableChild 父子结构委托等安全基线不因本待办放宽。

## 二、图形化列表数据范围

### 2.1 需要支持的规则

- 本人创建、本人负责、本人协作、本人所在部门。
- 本部门、平级部门、直属下级部门、全部下级部门、指定部门及其下级。
- 指定角色、当前角色、角色层级。
- 指定人员、人员集合、组织机构集合。
- 当前租户、业务组织、区域、项目、客户、团队等业务维度。
- 固定字段条件：等于、不等于、大于、小于、包含、开始于、结束于、为空、非空、In、NotIn。
- 多个条件支持嵌套 `AND` / `OR` 分组。
- 支持主表条件和经过白名单验证的关联表条件。
- 支持日期、数字、枚举、部门、人员、角色等类型感知编辑器。

### 2.2 配置与编译

- 图形化规则保存结构化 JSON AST，不能直接把管理员输入当作 SQL。
- 后端根据数据库类型把 AST 编译为参数化查询，支持 MySQL、Oracle、SQL Server。
- 字段、表、关联关系必须来自真实 `diy_table`、`diy_field`、`sys_menu.JoinTables` 元数据。
- 禁止任意表名、字段名、函数名、排序表达式和 SQL 片段穿透编译器。
- 保留现有 `SqlWhere`、`SqlJoin` / `JoinTables` 作为兼容层：
  - 老配置继续执行。
  - 新图形化配置优先使用独立物理字段，不能塞进废弃的 `DiyConfig`。
  - 可提供“解析旧规则并生成图形化草稿”，无法可靠解析时保持只读兼容，不自动改写。

### 2.3 无 `_SysMenuId` 的安全合并

- 授权候选只取当前租户、当前有效用户、当前有效角色真正拥有且直接绑定目标表的菜单。
- 同一 Join 上下文的条件可以编译为 `(范围A) OR (范围B)`。
- 任一菜单无范围时，合并结果为整表访问。
- 不同 Join 上下文由专门的规则规划器转换为参数化 `EXISTS`、主键子查询或其它数据库可移植表达式。
- 在多 Join 规则规划器完成前，无法安全合并的请求保持失败关闭，并记录不含敏感数据的诊断日志。
- 不能信任客户端提交的角色、部门、菜单集合、规则 JSON 或“已授权”标志。

## 三、单条详情字段权限

### 3.1 目标

在保留“拥有同表菜单即可打开详情”的基础上，允许通过图形界面配置：

- 哪些角色、部门、人员可以或不可以查看某些字段。
- 哪些角色、部门、人员可以或不可以编辑某些字段。
- 根据当前记录内容决定字段是否可见、只读或脱敏。
- 多条件 `AND` / `OR`、嵌套条件组。
- 显式允许与显式拒绝的优先级。
- 脱敏方式：完全隐藏、固定掩码、保留前后若干位、哈希/摘要、仅显示是否存在。

### 3.2 后端强制执行

- 字段权限必须在 FormEngine 最终 SELECT 投影和最终返回序列化之前执行，不能只由 PC/UniApp 控件隐藏。
- 未授权字段默认不查询；因 SQL Join、排序或内部计算必须临时使用的字段，也不能出现在最终响应中。
- `GetDiyField`、`GetDiyFieldByDiyTables` 返回的元数据应同步标记或剔除不可见字段，避免泄露字段数据源 SQL、V8、默认值等配置。
- 编辑提交时，后端必须删除或拒绝客户端无权编辑的字段，不能因为前端没显示就默认可信。
- DataFilterV8、后端表单 V8 的执行顺序要明确定义，避免脱敏后业务代码取不到原值，或业务代码把敏感值重新写回响应。
- 导出、打印、消息模板、AI 数据分析、搜索索引、文件字段和开放 API 必须复用同一字段权限决策，不能各自实现一套。

### 3.3 决策优先级（待产品确认）

建议默认顺序：

1. 平台保护字段硬拒绝。
2. 显式拒绝规则。
3. 显式允许规则。
4. 菜单/角色默认字段权限。
5. 表单字段原有可见、只读配置。
6. 未配置时保持当前兼容行为。

需要进一步确认“多个角色规则冲突时，拒绝优先还是允许并集”，并在 UI 中明确展示最终效果。

## 四、建议的数据模型

不要在现有通用 JSON 口袋字段中继续堆叠。建议新增可版本化的物理配置实体，例如：

- `mci_data_policy`：策略主表，包含租户、目标表、菜单、策略类型、状态、优先级、版本。
- `mci_data_policy_rule`：结构化条件 AST 或规则节点。
- `mci_field_policy`：详情字段查看/编辑/脱敏策略。
- `mci_policy_subject`：角色、部门、人员、组织等授权主体。
- `mci_policy_version`：发布版本、发布人、发布时间、回滚信息和变更摘要。

最终命名、是否拆表及索引方案需结合现有数据库规范评审。所有平台级安全表使用 `mci_` 前缀并强制 `OsClient` 隔离。

## 五、缓存与性能

- 编译后的策略按 `OsClient + 用户/角色授权版本 + 表Id + 菜单Id + 操作类型 + 策略版本` 缓存。
- L1 只作短 TTL 优化；共享 Redis L2 或数据库是跨节点事实源。
- 菜单、角色、人员、部门、规则、字段策略变化后提升租户授权 `epoch`，使所有 API/Worker 节点自然失效。
- 列表请求不得逐记录、逐字段查询权限表；应一次得到已编译的行范围和字段投影。
- 详情字段权限可根据“用户上下文 + 当前记录必要判定字段”一次决策，避免 N+1。
- 编译失败、缓存反序列化失败或规则引用失效字段时失败关闭，并提供管理员可理解的规则诊断。
- 需要基准测试 500–1200 张表、1万–1.5万个字段、多个角色和复杂部门树的真实规模。

## 六、分布式与发布兼容

- 策略发布、缓存失效和迁移必须支持多 API/Worker 节点及滚动升级。
- 使用“草稿 → 校验 → 发布版本 → 原子切换”的流程；不能让用户保存一半规则后立即影响生产请求。
- 新旧节点短暂并存时，旧节点应继续理解旧 `SqlWhere/SqlJoin`，新节点可读取新策略；采用先扩展、后迁移、再收缩。
- 规则编译结果必须包含契约版本；版本不兼容时重新编译，不能错误复用旧缓存。
- 发布与回滚需要审计日志，但不得记录 Token、敏感字段值或完整个人数据。

## 七、管理界面建议

- 模块引擎增加【列表数据范围】图形化设计页。
- 表单/字段设计增加【详情字段权限】或在角色权限管理中提供矩阵入口。
- 提供“以某用户预览最终权限”，展示：
  - 命中的菜单和角色。
  - 最终列表范围摘要。
  - 详情可见、只读、可编辑、脱敏字段。
  - 冲突规则和最终优先级。
- 提供规则测试器：输入样例用户和样例记录，只返回判定过程摘要，不泄露用户无权查看的真实数据。
- 对旧 `SqlWhere/SqlJoin` 显示“兼容只读规则”标识，并提示是否可迁移。

## 八、安全验收最低断言

1. 删除 `_SysMenuId` 不能扩大列表可见范围。
2. 传入其它表、其它租户或无权菜单 Id 必须失败。
3. 多菜单同 Join、不同 Join、无范围菜单的合并结果正确。
4. 详情能打开，但不可见字段在网络响应、字段元数据、导出、打印、AI、日志中均不泄露。
5. 无权编辑字段即使手工构造 HTTP Body 也不能修改。
6. 角色、部门、人员和规则变更后，多节点无需重启即可按新版本生效。
7. Redis 短暂故障、节点重启和滚动升级不会把失败降级为“允许全部”。
8. 平台保护表、保护字段、租户隔离和 TableChild 父子边界继续生效。
9. MySQL、Oracle、SQL Server 参数化编译结果一致，无 SQL 注入。
10. 规则查询性能在真实规模数据集上满足明确的 P95/P99 指标。

## 九、实施前仍需确认的产品决策

- 详情字段权限冲突时采用“拒绝优先”还是“所有角色允许权限并集”。
- 字段权限是否同时控制搜索条件、排序、统计、聚合和数据源联动。
- 无 `_SysMenuId` 且多个不同 Join 范围时，首版采用失败关闭、主键子查询合并还是 `EXISTS` 规划器。
- 表级高级权限与菜单权限同时存在时，列表范围如何合并。
- 超级管理员是否默认跳过业务策略，还是也允许配置强制脱敏字段。
- 图形化规则是否支持自定义服务端 V8 扩展，以及扩展的超时、缓存、审计和失败策略。

