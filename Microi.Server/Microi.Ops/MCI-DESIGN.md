# Microi.Ops 界面设计

使用仓库 Microi.UI 的 Vue3 组件与语义主题变量；随 Ops 镜像编译交付，不依赖平台 API、CDN 或在线微服务才能加载。

主要路径：独立账号登录 → 运行总览 → 选择容器 → 核对摘要与停机范围 → 服务端持久任务 → 就绪检查或恢复。平台登录单列为“平台连接”，只影响日志同步。

覆盖桌面、窄屏、亮暗主题、空列表、加载、接口错误、验证码启用/关闭、任务恢复和 iframe 独立访问回退。每页可见独立 URL；密码、平台 Token 和 Docker 凭据不进入 URL、本地存储或父窗口消息。
