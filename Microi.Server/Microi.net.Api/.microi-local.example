iTdos

# Microi 本地环境配置模板
# -----------------------------------------------
# 使用方法：
#   复制本文件为 .microi-local（注意：.microi-local 已加入 .gitignore，不会提交 git）
#   将上方的环境名替换为你需要连接的环境，保存后重启服务即生效
#
# 优先级说明（高 → 低）：
#   1. IDE 强制设置（VS Code launch.json [环境名] 配置 / VS/Rider launchSettings.json 具名 Profile）
#   2. 本文件 .microi-local
#   3. 系统/用户级环境变量（Windows 系统属性）
# -----------------------------------------------

# 本地发布项目：dotnet clean && dotnet publish -c Release -o ./bin/Release/publish
# 本地运行编译后的项目：dotnet Microi.net.Api.dll --urls=http://0.0.0.0:8080
# 本地运行项目：dotnet run --launch-profile Microi.net.Api