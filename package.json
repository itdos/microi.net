{
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2",
    "prettier": "^3.5.3"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "microi.web/**/*.{js,vue,json,md}": [
      "prettier --write --ignore-unknown",
      "git add"
    ]
  }
}
